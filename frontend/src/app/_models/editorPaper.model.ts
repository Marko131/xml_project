export class EditorPaper {
  paperTitle: string;
  paperStatus: string;

  constructor(paperTitle: string, paperStatus: string) {
    this.paperTitle = paperTitle;
    this.paperStatus = paperStatus;
  }
}
